resources:
  docker-registry:
    type: docker-registry
    endpoints:
      - name: docker-auth
        value: secret-token
        username: dockeruser
        password: dockerpassword

steps:
  - script: docker pull
